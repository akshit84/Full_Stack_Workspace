// 1. Given an array [1, 2, 3, 4], use map() to create a new array with each element doubled.

const arr = [1, 2, 3, 4]

const output = arr.map((item) => item * 2)

console.log("Old array:- " + arr);
console.log("New Array:- " + output);

